label: "[GSB][ACL] Website User Admin"

modules:
  system:
    - 'feusermanagement'

resources:
  fe_users:
    permissions: ['read', 'write']
    fields: '*'
  fe_groups:
    permissions: ['read', 'write']
    fields: '*'
